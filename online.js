// Simulate premium check (replace with actual logic)
const hasPremium = false; // Set to true if the user has premium

// Redirect to Premium Tab if the user doesn't have premium
if (!hasPremium) {
    window.location.href = 'premium.html';
}
